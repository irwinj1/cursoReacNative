import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    logo:{
        resizeMode: 'contain',
        height:100,
        width:'100%',
        marginBottom:40,
        marginTop:20
    },
    register:{
        color: '#00a680',
        marginHorizontal:10,
    }
});